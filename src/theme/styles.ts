import { Styles } from '@chakra-ui/theme-tools'

const styles: Styles = {
  global: {
    body: {
      bgColor: 'base',
      fontFamily: `'Montserrat', sans-serif`,
    },
  },
}

export default styles
